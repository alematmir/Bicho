{
  "name": "bicho",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "node scripts/verify-remote.mjs",
    "dev:shop": "npm run dev --workspace=shop",
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "dev:admin": "npm run dev --workspace=admin",
    "dev:delivery": "npm run dev --workspace=delivery"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.5",
    "supabase": "^2.114.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
